# Website moved

Public site and Firebase Hosting for **mymultiverse.app** live in a dedicated repo:

**https://github.com/Rob971/mymultiverse-website**

That repo owns company pages, `/privacy`, `/terms`, `/invite`, and `/.well-known/*` for App Links.

This app repo keeps only the APK fingerprint helper: `scripts/print-android-apk-fingerprint.sh`.
